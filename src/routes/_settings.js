import express from "express";
const settingsRouter = express.Router();

// const _SettingsController = require("../controllers/_SettingsController");

// settingsRouter.get("/reset-db-seed", _SettingsController.reset);
// settingsRouter.get("/reset-status", _SettingsController.status);
// settingsRouter.get("/reset-log", _SettingsController.resetLog);

export { settingsRouter };
